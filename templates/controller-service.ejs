import Controller from '../../core/class/controller-service';
import loggerFactory from '../../core/factory/logger';

module.exports = class <%- kName %>Controller extends Controller {
  constructor() {
    super({
      defaultServiceId: '<%- service %>',
      logger: loggerFactory('controller', { message_prefix: 'controller <%- name %>' }),
    });
  }
};
